{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys\nd = json.load(sys.stdin)\np = d.get('tool_input', {}).get('file_path', '')\nenv_files = ['.env', '.env.local', '.env.production', 'credentials', 'secrets', 'app_config.json', 'appsettings.json']\nif any(e in p for e in env_files):\n    print(json.dumps({'systemMessage': '[SECURITY] Editing credentials/config file. Ensure nothing sensitive is committed to git.'}))\nsys.exit(0)\n\""
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys, re\nd = json.load(sys.stdin)\nti = d.get('tool_input', {})\np = ti.get('file_path', '')\nskip = ['node_modules', '.claude/', 'test/', '.md']\nif any(s in p for s in skip):\n    sys.exit(0)\ncontent = ti.get('content', '') or ti.get('new_string', '')\nif not content:\n    sys.exit(0)\nviolations = []\nqf = re.search(r'//\\s*(hack|workaround|band-?aid|quick\\s*fix|temp(orary)?\\s*fix|hotfix)\\b', content, re.IGNORECASE)\nif qf:\n    violations.append(qf.group(0).strip())\ntd = re.search(r'//\\s*(TODO|FIXME):\\s*(fix later|investigate later|come back|revisit|temporary)\\b', content, re.IGNORECASE)\nif td:\n    violations.append(td.group(0).strip())\nif violations:\n    print(json.dumps({'systemMessage': '[STEER] Detected ' + violations[0] + '. Quick-fix markers suggest a workaround rather than a root-cause fix. Consider addressing the underlying issue.'}))\n    sys.exit(0)\nsys.exit(0)\n\""
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys, re\nd = json.load(sys.stdin)\nti = d.get('tool_input', {})\np = ti.get('file_path', '')\nskip = ['.claude/settings.json', 'CLAUDE.md', 'SKILL.md', '.md', 'node_modules']\nif any(s in p for s in skip):\n    sys.exit(0)\ncontent = ti.get('content', '') or ti.get('new_string', '')\nif not content:\n    sys.exit(0)\npatterns = [\n    r'ignore\\s+(all\\s+)?previous\\s+instructions',\n    r'ignore\\s+(all\\s+)?above\\s+instructions',\n    r'you\\s+are\\s+now\\s+(a|an)',\n    r'system\\s*prompt\\s*:',\n    r'<\\s*system[_-]?prompt\\s*>',\n    r'IMPORTANT:\\s*override',\n    r'act\\s+as\\s+(root|admin|sudo)',\n]\nfor pat in patterns:\n    if re.search(pat, content, re.IGNORECASE):\n        print(json.dumps({'systemMessage': '[INJECTION] Potential prompt injection pattern detected. Review carefully (OWASP ASI01).'}))\n        break\nsys.exit(0)\n\""
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys\nd = json.load(sys.stdin)\np = d.get('tool_input', {}).get('file_path', '')\ncritical = '{{CRITICAL_FILES}}'.split(',')\ncritical = [c.strip() for c in critical if c.strip()]\nif any(c in p for c in critical):\n    print(json.dumps({'systemMessage': '[GUARD] Critical infrastructure file (schema/routes/config). Run /validate-change after editing.'}))\nsys.exit(0)\n\""
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Based on the current task context, estimate how many files this change is likely to touch. If the file being written is a markdown (.md) file (e.g. design docs, plans, progress notes), allow silently \u2014 documentation files are planning artifacts, not implementation. Otherwise, if the change implies modifications to more than 5 files (high blast radius), warn: '[GUARD] High blast radius \u2014 consider running /brainstorm first to plan the approach.' If the scope is contained, allow silently."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys, os\nd = json.load(sys.stdin)\np = d.get('tool_input', {}).get('file_path', '')\next_str = os.environ.get('WORKFLOW_CODE_EXTENSIONS', '.ts,.tsx,.py,.dart,.cs')\nextensions = tuple(e.strip() for e in ext_str.split(','))\nif p.endswith(extensions):\n    print(json.dumps({'systemMessage': '[REMIND] Source file modified. Run /validate-change before committing.'}))\nsys.exit(0)\n\""
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys, os\nd = json.load(sys.stdin)\np = d.get('tool_input', {}).get('file_path', '')\nif not p or 'session-files' in p:\n    sys.exit(0)\nsid = d.get('session_id', 'unknown')\ncwd = os.getcwd()\nrel = os.path.relpath(p, cwd) if p.startswith(cwd) else p\ntf = os.path.join(cwd, '.claude', f'session-files-{sid}.txt')\nexisting = set()\nif os.path.exists(tf):\n    with open(tf) as f:\n        existing = set(l.strip() for l in f if l.strip())\nif rel not in existing:\n    os.makedirs(os.path.dirname(tf), exist_ok=True)\n    with open(tf, 'a') as f:\n        f.write(rel + '\\n')\nsys.exit(0)\n\""
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys, os\nd = json.load(sys.stdin)\np = d.get('tool_input', {}).get('file_path', '')\nif not p:\n    sys.exit(0)\nskip_ext = ('.md', '.json', '.yaml', '.yml', '.lock', '.txt', '.csv', '.html', '.css', '.svg')\nif p.endswith(skip_ext):\n    sys.exit(0)\nskip_path = ['node_modules', 'test', 'tests', '__tests__', '.spec.', '_test.', '.test.', '.g.dart', '.freezed.']\nif any(s in p for s in skip_path):\n    sys.exit(0)\nif not os.path.exists(p):\n    sys.exit(0)\nwith open(p) as f:\n    lines = len(f.readlines())\nsoft = int(os.environ.get('WORKFLOW_FILE_SIZE_SOFT', '400'))\nhard = int(os.environ.get('WORKFLOW_FILE_SIZE_HARD', '600'))\nbn = os.path.basename(p).lower()\nif any(k in bn for k in ['controller', 'router', 'endpoint', 'screen', 'route']):\n    soft, hard = 300, 500\nelif any(k in bn for k in ['service', 'provider', 'repository']):\n    soft, hard = 350, 550\nif lines > hard:\n    print(json.dumps({'decision': 'block', 'reason': f'[SIZE] BLOCKED: {os.path.basename(p)} is {lines} lines (hard limit: {hard}). Extract before continuing.'}))\n    sys.exit(2)\nelif lines > soft:\n    print(json.dumps({'systemMessage': f'[SIZE] {os.path.basename(p)} is {lines} lines (soft limit: {soft}). Consider extraction.'}))\nsys.exit(0)\n\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$(git rev-parse --show-toplevel)/.claude/hooks/pyrun -c \"\nimport json, sys, subprocess\nd = json.load(sys.stdin)\ncmd = d.get('tool_input', {}).get('command', '')\nif 'git commit' not in cmd or '--amend' in cmd:\n    sys.exit(0)\ntry:\n    msg = subprocess.check_output(['git', 'log', '-1', '--format=%B'], text=True, timeout=5)\n    if 'Co-Authored-By' not in msg:\n        print(json.dumps({'systemMessage': '[COMPLIANCE] Last commit is missing Co-Authored-By trailer. Use /commit skill to ensure attribution.'}))\nexcept Exception:\n    pass\nsys.exit(0)\n\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/session-start.py"
          },
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/mcp-security-scan.py"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "auto|manual",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/pre-compact.py"
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "matcher": "backend-handler|code-reviewer|db-expert|frontend-handler|planner|security-reviewer|test-writer",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/subagent-start.py"
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/post-failure.py"
          }
        ]
      }
    ],
    "TaskCompleted": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/task-completed.py"
          }
        ]
      }
    ],
    "TeammateIdle": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/teammate-idle.py"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/prompt-submit.py"
          },
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/context-check.py"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/subagent-stop.py"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && .claude/hooks/pyrun .claude/hooks/session-end.py"
          }
        ]
      }
    ]
  }
}
