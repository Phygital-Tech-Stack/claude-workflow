{
  "event": "PreToolUse",
  "matcher": "Edit|Write",
  "hook": {
    "type": "prompt",
    "prompt": "Check if the file being edited matches any critical infrastructure file (schema, migrations, pubspec.yaml, package.json, nx.json, pyproject.toml, appsettings.json, *.csproj). If yes, warn: '[GUARD] Critical infrastructure file. Run /validate-change after editing and consider the blast radius.' If no, allow silently."
  }
}
